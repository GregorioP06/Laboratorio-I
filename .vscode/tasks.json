{
	"version": "2.0.0",
	"tasks": [
        {
            "label": "TS - Compile current file",
            "type": "shell",
            "command": "tsc --target esnext --outDir ${workspaceFolder}/out --sourceMap true ${relativeFile}",
            "problemMatcher": ["$tsc"],
        }
    ]
}