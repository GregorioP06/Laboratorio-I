{
	"version": "2.0.0",
	"tasks": [
        {
            "label": "TS - Compile current file",
            "type": "shell",
            "command": "tsc --strict --module nodenext --outDir ${workspaceFolder}/out --sourceMap ${relativeFile}",
            "problemMatcher": ["$tsc"],
        }
    ]
}